package com.example.playlistmarker.domain.player.use_cases.state

enum class UiAudioPlayerState {
    STATE_DEFAULT,
    STATE_PREPARED,
    STATE_PLAYING,
    STATE_PAUSED,
    STATE_COMPLETED,
    STATE_STOPPED
}